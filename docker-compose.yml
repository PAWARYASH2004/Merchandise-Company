version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: merchandise-company:latest
    ports:
      - "80:80"
    volumes:
      - .:/usr/share/nginx/html
    environment:
      - NODE_ENV=production